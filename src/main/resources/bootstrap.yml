# spring applicaton name should be the service name
# to be registered in the eureka discovery server
spring:
  application:
    name: eureka-server  
  cloud:
    config:
      uri: http://localhost:8888
# applicationc configuration is externalized in eureka-server.properties in config server